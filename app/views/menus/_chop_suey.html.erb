<h3 class="menu-category-big">Chop Suey</h3>
<div class="row">
  <% @chop_suey.each do |item| %>
      <div class="col-md-4 item-entry">
        <div class="item-title">
          <%= item.title %>
        </div>
        <div class="pull-right">
          <%= price_correction(item) %>
        </div>
      </div>
  <% end %>
</div>
